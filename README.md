## MongoDB to LevelDB

use MongoDB collection with levelDB as local cache.

### Usage

create config.json

    $ npm install
    $ node cmd.js --sync 

    $ node cmd.js --list
    $ node cmd.js ObjectID
    $ node cmd.js --browser ObjectID


